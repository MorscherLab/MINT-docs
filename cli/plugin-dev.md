# Plugin development with the `mint` CLI

The detailed plugin-development walkthrough lives in the **Plugin Development** track:

→ [Tutorials → First analysis plugin](/sdk/tutorials/first-analysis-plugin) — end-to-end build of a hello-world plugin
→ [API Reference → CLI reference](/sdk/api/cli-reference) — every `mint` subcommand and flag
→ [Plugin Development home](/sdk/concepts/) — concepts, recipes, frontend, operations

This page is preserved as a redirect from the original `/cli/plugin-dev` URL — the canonical content has moved.
